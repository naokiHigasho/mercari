.items.clearfix
  - @items03.each do |item|
    = render partial: 'shared/item-square200', locals: {item: item}
  = link_to_next_page @items03, next_icon, class: "page-arrow left-arrow", remote: true, :params => {:view_num => 3}
  = link_to_prev_page @items03, prev_icon, class: "page-arrow right-arrow", remote: true, :params => {:view_num => 3}
